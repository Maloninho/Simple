<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple - Café Bar</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --coffee-brown: #8B4513;
            --warm-beige: #F5F1E8;
            --cream: #FDF8F0;
            --clay-orange: #D2691E;
            --forest-green: #2F4F2F;
            --charcoal: #2C2C2C;
            --gold: #DAA520;
        }

        body {
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            color: var(--charcoal);
            background: var(--cream);
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(253, 248, 240, 0.95);
            backdrop-filter: blur(10px);
            z-index: 1000;
            transition: all 0.3s ease;
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
        }

        .logo {
            font-family: 'Playfair Display', serif;
            font-size: 2rem;
            font-weight: 600;
            color: var(--coffee-brown);
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .logo:hover {
            color: var(--clay-orange);
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--charcoal);
            font-weight: 400;
            transition: color 0.3s ease;
            position: relative;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--clay-orange);
            transition: width 0.3s ease;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            background: linear-gradient(135deg, var(--cream) 0%, var(--warm-beige) 100%);
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            right: -20%;
            width: 40%;
            height: 100%;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="20" cy="20" r="1" fill="%23D2691E" opacity="0.1"/><circle cx="80" cy="40" r="1.5" fill="%238B4513" opacity="0.1"/><circle cx="40" cy="70" r="1" fill="%23DAA520" opacity="0.1"/></svg>') repeat;
            animation: float 20s infinite linear;
        }

        @keyframes float {
            0% { transform: translateY(0px) rotate(0deg); }
            100% { transform: translateY(-100px) rotate(360deg); }
        }

        .hero-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            z-index: 2;
        }

        .hero-text h1 {
            font-family: 'Playfair Display', serif;
            font-size: 4rem;
            font-weight: 600;
            color: var(--coffee-brown);
            margin-bottom: 1rem;
            line-height: 1.2;
        }

        .hero-text p {
            font-size: 1.2rem;
            color: var(--forest-green);
            margin-bottom: 2rem;
            font-weight: 300;
        }

        .cta-button {
            display: inline-block;
            padding: 1rem 2rem;
            background: var(--clay-orange);
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 500;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(210, 105, 30, 0.3);
        }

        .cta-button:hover {
            background: var(--coffee-brown);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(139, 69, 19, 0.4);
        }

        .hero-visual {
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        .coffee-illustration {
            width: 300px;
            height: 300px;
            background: var(--coffee-brown);
            border-radius: 50%;
            position: relative;
            animation: pulse 4s ease-in-out infinite;
            box-shadow: 0 20px 40px rgba(139, 69, 19, 0.2);
        }

        .coffee-illustration::before {
            content: '☕';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 4rem;
            filter: sepia(1) hue-rotate(20deg);
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        /* About Section */
        .about {
            padding: 5rem 0;
            background: var(--warm-beige);
        }

        .about-content {
            text-align: center;
            max-width: 800px;
            margin: 0 auto;
        }

        .about h2 {
            font-family: 'Playfair Display', serif;
            font-size: 3rem;
            color: var(--coffee-brown);
            margin-bottom: 2rem;
        }

        .about-text {
            font-size: 1.3rem;
            line-height: 1.8;
            color: var(--forest-green);
            font-weight: 300;
            font-style: italic;
            margin-bottom: 3rem;
        }

        .values-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .value-card {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
            border-left: 4px solid var(--clay-orange);
        }

        .value-card:hover {
            transform: translateY(-10px);
        }

        .value-card h3 {
            font-family: 'Playfair Display', serif;
            color: var(--coffee-brown);
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }

        /* Menu Section */
        .menu {
            padding: 5rem 0;
            background: var(--cream);
        }

        .menu h2 {
            font-family: 'Playfair Display', serif;
            font-size: 3rem;
            color: var(--coffee-brown);
            text-align: center;
            margin-bottom: 3rem;
        }

        .menu-categories {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 3rem;
        }

        .menu-category {
            background: white;
            padding: 2rem;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            transition: all 0.3s ease;
        }

        .menu-category:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 45px rgba(0,0,0,0.15);
        }

        .menu-category h3 {
            font-family: 'Playfair Display', serif;
            color: var(--clay-orange);
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
            text-align: center;
        }

        .menu-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
            border-bottom: 1px solid var(--warm-beige);
        }

        .menu-item:last-child {
            border-bottom: none;
        }

        .item-name {
            font-weight: 500;
            color: var(--charcoal);
        }

        .item-price {
            color: var(--coffee-brown);
            font-weight: 600;
        }

        /* Contact Section */
        .contact {
            padding: 5rem 0;
            background: var(--forest-green);
            color: white;
        }

        .contact h2 {
            font-family: 'Playfair Display', serif;
            font-size: 3rem;
            text-align: center;
            margin-bottom: 3rem;
            color: var(--gold);
        }

        .contact-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
        }

        .contact-info h3 {
            font-family: 'Playfair Display', serif;
            color: var(--gold);
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }

        .contact-info p {
            margin-bottom: 1rem;
            font-weight: 300;
        }

        .map-placeholder {
            background: var(--warm-beige);
            border-radius: 15px;
            height: 300px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--charcoal);
            font-size: 1.2rem;
        }

        /* Footer */
        footer {
            background: var(--charcoal);
            color: white;
            text-align: center;
            padding: 2rem 0;
        }

        .social-links {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin-bottom: 1rem;
        }

        .social-links a {
            display: inline-block;
            width: 40px;
            height: 40px;
            background: var(--clay-orange);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            text-decoration: none;
            transition: background 0.3s ease;
        }

        .social-links a:hover {
            background: var(--gold);
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .hero-content {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .hero-text h1 {
                font-size: 2.5rem;
            }

            .contact-content {
                grid-template-columns: 1fr;
            }

            .coffee-illustration {
                width: 200px;
                height: 200px;
            }

            .coffee-illustration::before {
                font-size: 3rem;
            }
        }

        /* Smooth scrolling */
        html {
            scroll-behavior: smooth;
        }

        /* Scroll animations */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.6s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <header>
        <nav class="container">
            <a href="#" class="logo">Simple</a>
            <ul class="nav-links">
                <li><a href="#inicio">Inicio</a></li>
                <li><a href="#nosotros">Nosotros</a></li>
                <li><a href="#menu">Menú</a></li>
                <li><a href="#contacto">Contacto</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <section id="inicio" class="hero">
            <div class="container">
                <div class="hero-content">
                    <div class="hero-text">
                        <h1>Simple</h1>
                        <p>Redescubriendo la belleza de lo esencial</p>
                        <a href="#nosotros" class="cta-button">Descubre más</a>
                    </div>
                    <div class="hero-visual">
                        <div class="coffee-illustration"></div>
                    </div>
                </div>
            </div>
        </section>

        <section id="nosotros" class="about">
            <div class="container">
                <div class="about-content fade-in">
                    <h2>Nuestra Esencia</h2>
                    <div class="about-text">
                        <p>Simple es un lugar donde redescubriremos la belleza de lo esencial.</p>
                        <p>Aquí, lo sencillo cobra sentido y se transforma en momentos memorables.</p>
                        <p>Un lugar de risas, miradas que conectan, charlas que fluyen sin prisa.</p>
                        <p>Encuentros con conocidos y desconocidos, contigo.</p>
                        <p>Simple es un espacio donde lo cotidiano se vuelve extraordinario.</p>
                    </div>
                </div>
                
                <div class="values-grid">
                    <div class="value-card fade-in">
                        <h3>Artesanal</h3>
                        <p>Cada taza es preparada con dedicación y técnicas tradicionales colombianas, honrando el arte del café.</p>
                    </div>
                    <div class="value-card fade-in">
                        <h3>Conexión</h3>
                        <p>Creamos espacios para encuentros genuinos, donde las conversaciones fluyen naturalmente.</p>
                    </div>
                    <div class="value-card fade-in">
                        <h3>Simplicidad</h3>
                        <p>Encontramos la belleza en lo esencial, donde cada detalle tiene propósito y significado.</p>
                    </div>
                </div>
            </div>
        </section>

        <section id="menu" class="menu">
            <div class="container">
                <h2 class="fade-in">Nuestro Menú</h2>
                <div class="menu-categories">
                    <div class="menu-category fade-in">
                        <h3>Cafés Especiales</h3>
                        <div class="menu-item">
                            <span class="item-name">Café Simple</span>
                            <span class="item-price">$4.500</span>
                        </div>
                        <div class="menu-item">
                            <span class="item-name">Cappuccino Artesanal</span>
                            <span class="item-price">$6.000</span>
                        </div>
                        <div class="menu-item">
                            <span class="item-name">Café de Origen</span>
                            <span class="item-price">$7.500</span>
                        </div>
                        <div class="menu-item">
                            <span class="item-name">Cold Brew</span>
                            <span class="item-price">$5.500</span>
                        </div>
                    </div>
                    
                    <div class="menu-category fade-in">
                        <h3>Acompañamientos</h3>
                        <div class="menu-item">
                            <span class="item-name">Croissant Simple</span>
                            <span class="item-price">$3.500</span>
                        </div>
                        <div class="menu-item">
                            <span class="item-name">Tostada Artesanal</span>
                            <span class="item-price">$4.000</span>
                        </div>
                        <div class="menu-item">
                            <span class="item-name">Galletas de la Casa</span>
                            <span class="item-price">$2.500</span>
                        </div>
                        <div class="menu-item">
                            <span class="item-name">Muffin del Día</span>
                            <span class="item-price">$3.000</span>
                        </div>
                    </div>
                    
                    <div class="menu-category fade-in">
                        <h3>Bebidas Especiales</h3>
                        <div class="menu-item">
                            <span class="item-name">Chocolate Caliente</span>
                            <span class="item-price">$5.000</span>
                        </div>
                        <div class="menu-item">
                            <span class="item-name">Té de Hierbas</span>
                            <span class="item-price">$4.000</span>
                        </div>
                        <div class="menu-item">
                            <span class="item-name">Limonada Natural</span>
                            <span class="item-price">$3.500</span>
                        </div>
                        <div class="menu-item">
                            <span class="item-name">Agua Aromática</span>
                            <span class="item-price">$2.500</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section id="contacto" class="contact">
            <div class="container">
                <h2 class="fade-in">Encuéntranos</h2>
                <div class="contact-content">
                    <div class="contact-info fade-in">
                        <h3>Ubicación</h3>
                        <p>Calle 85 #12-34<br>Zona Rosa, Bogotá</p>
                        
                        <h3>Horarios</h3>
                        <p>Lunes a Viernes: 7:00 AM - 9:00 PM<br>
                        Sábados y Domingos: 8:00 AM - 10:00 PM</p>
                        
                        <h3>Contacto</h3>
                        <p>Teléfono: +57 (1) 234-5678<br>
                        Email: hola@simple.cafe</p>
                        
                        <h3>Síguenos</h3>
                        <p>@simple.cafe</p>
                    </div>
                    <div class="map-placeholder fade-in">
                        <p>📍 Mapa Interactivo<br>Te esperamos en el corazón de la ciudad</p>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer>
        <div class="container">
            <div class="social-links">
                <a href="#">📘</a>
                <a href="#">📷</a>
                <a href="#">🐦</a>
            </div>
            <p>&copy; 2025 Simple Café Bar. Todos los derechos reservados.</p>
        </div>
    </footer>

    <script>
        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Fade in animation on scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        // Observe all fade-in elements
        document.querySelectorAll('.fade-in').forEach(el => {
            observer.observe(el);
        });

        // Header background change on scroll
        window.addEventListener('scroll', () => {
            const header = document.querySelector('header');
            if (window.scrollY > 100) {
                header.style.background = 'rgba(253, 248, 240, 0.98)';
                header.style.boxShadow = '0 2px 20px rgba(0,0,0,0.1)';
            } else {
                header.style.background = 'rgba(253, 248, 240, 0.95)';
                header.style.boxShadow = 'none';
            }
        });

        // Add subtle parallax effect to hero
        window.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            const hero = document.querySelector('.hero');
            if (hero) {
                hero.style.transform = `translateY(${scrolled * 0.5}px)`;
            }
        });
    </script>
</body>
</html>
